package com.amosh.pulse.ui.theme

import androidx.compose.ui.graphics.Color

val colorPrimary get() = primary
val colorAccent get() = accent

val white = Color.White
val black = Color.Black
val translucentBlack = Color(0x40000000)
val transparentWhite = Color(0x90FFFFFF)
val transparentBlack = Color(0x90000000)

val primary = Color(0xFF6A4DFF)
val accent = Color(0xFFFFC400)


val primaryDark = Color(0xFFB2A5FF)
val accentDark = Color(0xFFFFE066)